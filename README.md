# Oil Spill Project
```mermaid
flowchart LR

    Dev[Developer запускает Spring Boot]

    subgraph Local files
        ENV[".env (секреты, не в Git)"]
        YAML["application.yml (плейсхолдеры)"]
        DOCKER["docker-compose.yml"]
    end

    ENV -->|env vars| Spring[Spring Boot]
    YAML -->|${JDBC_URL} ${DB_USER}| Spring

    ENV -->|env vars| Docker[Docker Compose]
    DOCKER --> Docker

    Docker --> Postgres[(PostgreSQL container)]
    Spring --> Postgres
```

🔧 Local development setup (IntelliJ IDEA)
Environment variables

Проект использует переменные окружения для конфигурации подключения к базе данных
(логины и пароли не хранятся в репозитории).

Для локального запуска используется файл .env, который не коммитится.

1. Создать .env

В корне проекта создать файл .env

Пример файла доступен в репозитории: .env.example.
Указать свои корректные данные для подключения к базе данных

2. IntelliJ IDEA: подключение .env файла

IntelliJ IDEA не подхватывает .env автоматически, поэтому для корректного запуска
необходимо подключить файл через плагин.

Установка плагина EnvFile

Settings → Plugins → Marketplace

Найти EnvFile

Установить и перезапустить IDE

Настройка Run Configuration

Run → Edit Configurations

Выбрать конфигурацию Spring Boot

Включить опцию EnvFile

Добавить файл .env

Убедиться, что включены:

Enable EnvFile

Substitute environment variables

После этого Spring Boot будет автоматически получать все переменные окружения из .env.

3. Запуск базы данных

Для локального запуска PostgreSQL используется Docker Compose:

docker compose up -d


Docker также использует переменные из .env.

4. Запуск приложения

После выполнения шагов выше приложение можно запускать обычным способом:

через кнопку Run в IntelliJ IDEA

или через Maven / Gradle

ℹ️ Важно

Файл .env должен оставаться в .gitignore

В репозитории хранится только .env.example

Все чувствительные данные остаются локально